# Core
moss==1.4.0
# IMPORTANT: moss 1.4.0 pins inferedge-moss-core==0.14.0, but that version makes
# session.push_index() fail with HTTP 400 "property modelId should not exist".
# Force the newer core, which fixes the push payload. (pip will warn about the
# pin mismatch — that's expected and safe.)
inferedge-moss-core==0.17.0
python-dotenv

# Voice agent (LiveKit). Pinned to 1.2.9 because livekit-plugins-minimax==1.3.0
# hard-pins livekit-agents==1.2.9; all plugins must match that line.
livekit-agents==1.2.9
livekit-plugins-minimax==1.3.0
livekit-plugins-deepgram==1.2.9
livekit-plugins-silero==1.2.9
livekit-plugins-openai==1.2.9

# Memory service (FastAPI) — used later
fastapi
uvicorn[standard]
